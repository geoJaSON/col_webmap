node_modules/
.next/
out/
build/
# Every env file except the template -- a stray .env.bak or .env.real
# holds live credentials just as surely as .env does.
.env*
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts
__pycache__/
.DS_Store

# Lock files a spreadsheet app leaves while a workbook is open --
# LibreOffice (.~lock.<name>#) and Excel (~$<name>). One was committed once.
.~lock.*#
~$*
